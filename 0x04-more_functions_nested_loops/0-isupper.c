#include"main.h"

/**
 * _isupper - check if c is upper
 * @c: input for alphabet
 * Return: 1 if it is upper, 0 otherwise
*/

int _isupper(int c)
{
if (isupper(c))
{
return (1);
else
return (0);
}
}
